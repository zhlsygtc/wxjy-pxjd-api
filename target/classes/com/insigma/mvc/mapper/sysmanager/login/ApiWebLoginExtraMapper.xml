<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.insigma.mvc.dao.sysmanager.login.ApiWebLoginExtraMapper">
    <!-- 更新教室信息 -->
     <update id="updPassword" parameterType="SUser">
    	update 
    		s_user
    	set 
    		password = #{password}
    	where 
    		userid = #{userid}
    </update>
</mapper>